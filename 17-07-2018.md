## Conda support to switch kernels for Jupyter
```
conda install nb_conda
```

## Cloning a repository into current directory
```
git clone <URL> .
```

## Finding a process that is running in a particular port
```
sudo ss -lptn 'sport = :<PORT_NUMBER>'
```
